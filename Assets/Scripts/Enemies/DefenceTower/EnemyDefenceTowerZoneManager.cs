using UnityEngine;

public class EnemyDefenceTowerZoneManager : MonoBehaviour
{
    public EnemyDefenceTowerManager enemyDefenceTowerManager;

    public Transform enemyDefenceTower;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
